# 可可语伴 AI 客服系统 —— 前端手动验收指引

## 操作前确认

- [x] 后端服务已启动（`http://localhost:8000`）
- [x] 前端服务已启动（`http://localhost:5173`）
- [ ] 浏览器已打开无痕窗口

---

## 场景一：新用户首次使用

| 步骤 | 操作 | 预期结果 | 实际结果 |
| :--- | :--- | :--- | :--- |
| 1 | 访问 `http://localhost:5173/chat` | 自动跳转至 `/login` | ☐ |
| 2 | 输入 `admin@app.com` / `123456`，点击登录 | 跳转至 `/chat`，显示左侧会话列表和右侧聊天区域 | ☐ |
| 3 | 输入"会员多少钱？"，点击发送 | 收到回答，含"68元/月" | ☐ |
| 4 | 刷新页面 | 聊天记录仍存在，会话列表不丢失 | ☐ |

## 场景二：多轮对话 + 指代消解

| 步骤 | 操作 | 预期结果 | 实际结果 |
| :--- | :--- | :--- | :--- |
| 1 | 输入"可可语伴会员多少钱？" | 正常回答 | ☐ |
| 2 | 输入"它贵吗？" | 正确指代，回答"68元/月" | ☐ |

## 场景三：新会话按钮交互

| 步骤 | 操作 | 预期结果 | 实际结果 |
| :--- | :--- | :--- | :--- |
| 1 | 当前会话有消息，点击"+ 新会话" | 新建空白会话，切换选中 | ☐ |
| 2 | 在新会话中输入消息 | 消息发送成功，显示 AI 回复 | ☐ |

## 场景四：日志查询

| 步骤 | 操作 | 预期结果 | 实际结果 |
| :--- | :--- | :--- | :--- |
| 1 | 点击顶部导航"日志" | 进入日志列表页 | ☐ |
| 2 | 查看列表是否显示历史请求 | 显示最近聊天记录（trace_id、question、intent） | ☐ |
| 3 | 点击某条日志查看详情 | 显示各节点详情（intent_recognition / retrieval / rerank / llm_generation） | ☐ |

## 场景五：异常降级

| 步骤 | 操作 | 预期结果 | 实际结果 |
| :--- | :--- | :--- | :--- |
| 1 | 临时关闭后端服务，刷新前端 | 页面显示"暂无会话"，不白屏 | ☐ |
| 2 | 尝试发送消息 | 提示"服务繁忙"或消息发不出去 | ☐ |
| 3 | 重启后端，刷新页面 | 恢复正常 | ☐ |

## 场景六：Token 过期处理

| 步骤 | 操作 | 预期结果 | 实际结果 |
| :--- | :--- | :--- | :--- |
| 1 | 清空 `localStorage` 或删除 token（F12 → Application → Local Storage） | 自动跳转至 `/login` | ☐ |
| 2 | 重新登录 | 恢复正常使用 | ☐ |

## 验收结论

- [ ] 全部通过
- [ ] 存在 Bad Case（记录到 `BadCase.md`）
